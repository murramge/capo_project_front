import * as React from "react";

interface ISortCategoryProps {}

const SortCategory: React.FunctionComponent<ISortCategoryProps> = (props) => {
  return <div className="bg-red-300">정렬</div>;
};

export default SortCategory;
